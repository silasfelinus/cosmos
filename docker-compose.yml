services:
  kind-robots:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cosmos
    ports:
      - "3009:3000"
    expose:
      - 3000
    volumes:
      - /mnt/user/appdata/cosmos:/
    restart: unless-stopped
    environment:

    labels:
      -  "traefik.enable: true"
      -  "traefik.http.routers.cosmos.entryPoints: https"
    networks:
      - cafepurr
networks:
  cafepurr:
    external: true
    name: cafepurr